# 实时语音交互方案选型

## 一、背景与目标

构建一个可实时对话的语音应用：用户开口说话，系统能"听懂—思考—回应—说出来"，并支持打断、追问、长上下文等自然对话行为。

**部署前提**：本地（私有化）部署开源模型，无供应商锁定。

核心评估维度：
- **延迟（Latency）**：从用户说完到系统开始出声（First-Byte-Audio），目标 < 1.5s。
- **自然度**：能否识别停顿/语气/打断，回复是否流畅、有节奏感。
- **工程复杂度**：模型部署链路、流式推理实现、运维监控的难度。
- **算力成本**：所需显卡数量 / 显存 / 吞吐。
- **可替换性**：每个模块的候选模型与切换成本。

---

## 二、候选方案

### 方案 ① 前端 VAD + 非流式 ASR + 流式LLM + 非流式 TTS

**架构**

```
浏览器(录音 + VAD 端点检测)
        │  [音频 PCM]
        ▼
本地 ASR（非流式，本地汇总一句话整体后包成wav发给asr模型）
        │  [文本]
        ▼
本地 LLM（流式输出）
        │  [完整文本]
        ▼
本地 TTS（非流式，按句子切分后依次发给TTS）
        │  [每句完整音频]
        ▼
浏览器播放（逐句播放）
```

**实现要点**
- 浏览器侧用 Silero VAD 检测端点（停顿 ≥ 600ms 视为一句话结束）。
- 上传完整音频片段，ASR 一次返回文字。
- LLM 生成完整回复，再切成句子送 TTS（提升响应速度）。
- 浏览器拿到完整音频后 `<audio>` 播放。


**优点**
- 工程量最小，模型选型成熟（Paraformer、Qwen、CosyVoice 都是开源主流）。
- 模块解耦，单独替换/升级影响面小。
- 单轮推理可以选最准的模型，不必为流式妥协。
- 出错定位容易，监控粒度细。

**缺点**
- 延迟最大：语音输入、asr、llm、tts整个流程是串行的。
- 打断逻辑需要自己实现，不好控制。
- 多段对话需要显式轮次管理（VAD 端点即轮次）。

---

### 方案 ② 前端 VAD + 非流式 ASR + 流式 LLM + 流式 TTS

**架构**

```
浏览器(录音 + VAD 端点检测)
        │  [整句 PCM]
        ▼
本地 ASR（非流式，整句一次返回）
        │  [文本]
        ▼
本地 LLM（流式输出 token）
        │  [增量 token → 按标点切句]
        ▼
本地流式 TTS（边合成边出音频 chunk）
        │  [音频流]
        ▼
浏览器流式播放（Web Audio API 排队播放）
```

**实现要点**
- 浏览器侧用 Silero VAD 检测端点（停顿 ≥ 600ms 视为一句话结束），把整句音频一次性送 ASR。
- ASR 用整句识别（非流式），回传完整文本+句末标点。
- 流式输入TTS大多不开源，且流式输入意义不大，这里的流式指流式输出。LLM 流式输出 token，按标点/停顿切句，第一句立刻送流式 TTS，无需等全文。
- 流式 TTS（如 CosyVoice / Fish-Speech）逐句边合成边吐音频 chunk。
- 浏览器用 Web Audio API 边收 chunk 边入队播放。

**优点**
- 延迟比 ① 显著降低（首字节音频 < 1.5s）。
- ASR 用最准的整句模型即可（SenseVoice-Small / FunASR-Nano 都能直接用），不用改造流式接口。
- 模块仍解耦，每个模块可独立升级/替换。
- ASR 链路简单：无 WebSocket 长连接、无流式 ASR 的并发压力，部署门槛低。

**缺点**
- ASR 仍是串行瓶颈：必须等用户说完一整句才开始处理，长句/思考停顿会被一并延迟。
- LLM 第一句必须等 ASR 整句返回后才能开始流式，第一句延迟仍受 ASR 推理时间影响。
- 打断逻辑要贯穿 LLM + TTS 两段流式链路，工程一致性挑战中等。


---

### 方案 ③ 流式 ASR + 流式LLM + 流式 TTS

**架构**

```
浏览器(流式录音 + VAD)
        │  [音频流 / chunk]
        ▼
本地流式 ASR（如Qwen3-ASR-Realtime）
        │  [增量文本 + 端点事件]
        ▼
本地 LLM 流式生成
        │  [增量 token]
        ▼
本地流式 TTS（CosyVoice-streaming）
        │  [音频流]
        ▼
浏览器流式播放（Web Audio API 排队播放）
```

**实现要点**

- 流式 ASR：按 100-200ms chunk 送入，持续返回 partial/final。
- 流式 TTS：流式输入TTS大多不开源，且流式输入意义不大，这里的流式指流式输出。按 LLM 输出的句末标点/停顿切句，送入 CosyVoice 流式推理，输出音频 chunk。
- 客户端用 Web Audio API 边收音频 chunk 边入队播放。
- 打断：abort LLM 输出 + 丢弃未播放 TTS chunk + 关闭 VAD 收声。

**ASR → LLM 触发策略（关键设计点）**
- **partial 不送 LLM**：识别文本随识别推进会变（"今天天气" → "今天天气不错" → "今天天气不错，适合出门"），每段 partial 都送会让 LLM 反复重新生成。
- **final 才送 LLM**，触发来源三选一：
  1. **客户端 VAD 长静音**（如 ≥ 600ms）：粗端点，反应快但易误切
  2. **ASR 句末标点**（`。！？.!`）：语义边界准，但依赖标点（口语化无标点时失效）
  3. **ASR 内部静音检测**：细端点，比客户端 VAD 准
- **多源融合**：双触发取先到者，任一条件触发即送 LLM。任一条件 = `文本长度 ≥ 2-3 字 + (有句末标点 | 长静音 | 兜底超时 2-3s)`。
- **防误触**：触发冷却（触发后 N 毫秒内不重复）、最小长度门槛、低置信度 final 不触发。
- **复杂场景**：无标点长句靠兜底超时强切；用户说完但 ASR 未识别完时 VAD 先触发、ASR 后续 final 补字幕；句中停顿 < 阈值不触发。


**优点**
- 延迟显著降低（首字节音频 1-1.5s）。
- 语音输入和asr并行，llm和tts并行。
- 模块仍解耦，可独立升级 ASR/LLM/TTS。
- 可根据上下文改写输出的文本，输出准确率高。

**缺点**
- 流式 TTS 起步延迟（凑齐一句话才合成）是体验瓶颈。
- 句末切分是个坑：切碎语调碎，切慢没流式感。
- 打断逻辑要贯穿三个模块，工程一致性挑战大。
- 对接asr需要websocket长连接,有并发压力

---

### 方案 ④ 端到端实时语音交互大模型

**架构**

```
浏览器(录音 + VAD + 打断检测)
        │  [原始音频流]
        ▼
本地端到端语音大模型（Qwen-Audio-Realtime）
   - 自带 ASR + LLM + TTS 联合推理
        │  [音频流 + 文本流]
        ▼
浏览器流式播放
```

**实现要点**
- 一次 WebSocket/RTC 连接，客户端持续上推 PCM、下拉音频 chunk。
- 模型内部负责 ASR + 语义理解 + LLM + TTS。
- 文本流可选：很多端到端模型会并行输出 transcript 和 audio，便于前端字幕。
- 打断/轮次：客户端 VAD + 模型自带的 turn-taking 信号。

**优点**
- 延迟最低（首字节音频 500ms-1s）。
- 天然处理打断、语气、笑声，整体自然度最高。
- 模块最少，客户端实现最简。

**缺点**
- 端到端开源模型仍是少数派，部署文档/社区支持薄弱。
- 单卡显存要求高（A100/H100 起步）。
- 想插入 RAG/工具调用/领域知识很困难。

**适用场景**
- 对话自然度优先、产品定位高端。
- 单 GPU 算力足够、团队熟悉多模态部署。
- 接受"模型选型即终态"的事实。

---

### 横向对比

| 维度 | ① 前端VAD + 非流式 | ② 非流式 ASR + 流式 LLM/TTS | ③ 流式 ASR/LLM/TTS | ④ 端到端 Realtime |
|------|-------------------|------------------------------|--------------------|------------------|
| 自然度 | 机械，分段感强 | 较自然（句内流式） | 较自然 | 最接近真人 |
| 打断支持 | 难 | 可做，中等复杂度 | 可做但复杂 | 原生支持 |
| 模块解耦 | 完全解耦 | 完全解耦 | 完全解耦 | 黑盒，一体化 |
| 算力（GPU 张数） | 1-2 张（可分时复用） | 1-2 张（可分时复用） | 2-3 张高性能卡 | 1-2 张 A100/H100 |
| 显存峰值 | ~24G 单卡可串行 | ~24G 单卡可串行 | ~40G 跨卡 | ~24G 单模型 |
| 长连接层数 | 1（仅客户端 WS） | 1-2（客户端 WS，可选上游 TTS 长连） | 2（客户端 + 上游 ASR/TTS ） | 2（客户端 + 上游模型）|
| 工程复杂度 | **低** | **中** | **高** | **中** |
| 部署框架成熟度 | 高（vLLM/CosyVoice 都稳） | 高（ASR 直接用整句模型即可） | 中（流式 ASR 需改造） | 低（多模态流式仍在演进） |
| 监控/调优粒度 | 细（每个模块独立） | 细 | 细 | 粗（端到端黑盒） |
| 故障定位 | 简单 | 简单-中 | 中等 | 困难 |
| 单次对话算力成本 | 低 | 低 | 中 | 中-高（GPU 长期占用） |
| 适合 MVP 起步 | ★★★★★ | ★★★★★ | ★★★ | ★★ |
| 适合长期生产 | ★★ | ★★★★ | ★★★★ | ★★★ |

---

综合评估下来，选择非流式asr+llm+流式tts。

## 三、模型选型
### 3.1 ASR

> **前提调整**：综合评估选择方案 ②（**非流式 ASR + 流式 LLM + 流式 TTS**），ASR 不再硬性要求流式。
>
> 端点检测可由前端 VAD（Silero VAD 等）或 ASR 内置 VAD 承担，ASR 把切好的整句音频转成文字送 LLM。
>
> 评估优先级：**情感识别（SER）≈ 算力/并发 > 中文效果（精度）> 部署复杂度 > 流式能力（加分项）**
>
> 数据来源：[FunASR 模型选择指南](https://www.funasr.com/blog/which-funasr-model.html)、[FunASR-Nano 与 SenseVoice 对比](https://www.modelscope.cn/models/iic/funasr-nano)

#### 候选模型概览

| 模型 | 形态 | 类型 | 部署方式 | 候选状态 |
|------|------|------|----------|---------|
| **FunASR-Nano** | 本地开源 | 端到端 ASR（含方言/歌词/Rap/热词/ITN/智能体交互） | 本地 | ✅ 高精度备选（无 SER） |
| **SenseVoice-Small** | 本地开源 | 非自回归多任务（ASR + SER + AED + LID） | 本地 | ✅ **首推（高并发 + SER）** |
| Whisper（large-v3 / Turbo） | 本地开源 | 自回归，整句 | 本地 | ⚠️ 备选（英文优先） |

#### 详细对比

**FunASR-Nano（阿里通义实验室 / FunAudioLLM，2025）**
- 模型规格：约 800M 参数（架构 = SenseVoice 编码器 + Qwen3-0.6B 解码器）；FP16 约 ~1.5GB，**GGUF 量化版 ~484MB**
- 核心定位：主打高精度 ASR，**不输出情感/事件标签**（与 SenseVoice 路线不同）
- 中文效果：AIShell1 WER **1.80%**（Whisper-large-v3 是 4.72%，领先约 20%）；真实噪声场景准确率 93%
- 推理特征：CPU/GPU 都跑得动，vLLM 加速可达 ~340× 实时
- 内置 VAD/标点：**有**（集成端点检测 + NT 标点预测 + 时间戳）
- 智能体交互：支持 `assistant` 特殊 token，可输出 `<think>` 思考内容（独特卖点）
- **热词（hotwords）**：✅ **原生支持**（`hotwords=["开放时间", "发布会"]` 参数；GGUF 版额外支持 CTC 预识别 + 音素模糊匹配 + 热词文件运行时实时修改）
- **上下文（context）**：✅ **LLM 级语义上下文**——Qwen3-0.6B 解码器提供语言模型级理解（同音消歧、不完整句、专有名词语义绑定）；但**仅 Python `AutoModel.generate()` 原生支持** 4 个参数（`context` / `prompt` / `system` / `chat_history`），**WebSocket 和 OpenAI 兼容 HTTP 接口都不支持**这些私有参数
- 输出格式：Jsonl（含时间戳、文本、Token ID）
- 多任务能力（与 SenseVoice **不同**）：
  - ✅ 歌词识别（强背景音乐）
  - ✅ Rap 语音识别
  - ✅ 热词增强（提升专有名词召回）
  - ✅ 说话人分离（需配合 CAM++）
  - ✅ 逆文本正则化（ITN，自动"一百二十三"→"123"）
  - ✅ 语种识别（中/英/日）
  - ❌ **情感识别（SER）**——本模型**不支持**
  - ❌ **声音事件检测（BED / AED）**——本模型**不支持**
- 多语言：中文（7 大方言 + 26 种口音）、英文、日文；MLT 版本支持 31 种语言
- 优点：精度 SOTA、方言口音覆盖强、歌词/Rap 识别、GGUF 量化 <500MB、智能体交互
- 缺点：**没有 SER/AED**，如需情感/事件识别需另接 SenseVoice 或 emotion2vec
- 适用：**当前场景主选**——精度、速度、方言、热词、智能体交互兼顾

**SenseVoice-Small（阿里达摩院 / FunAudioLLM，2024）**
- 模型规格：约 230M 参数，**Int8 量化后约 237MB**
- 核心定位：**多模态语音理解与感知**——ASR + 语种识别 + 情感识别（SER）+ 声音事件检测（AED）
- 中文效果：AISHELL-1 等纯 ASR 测试集上略低于 FunASR-Nano（上一代模型，被 Nano 替代趋势明显）
- 推理特征：非自回归多任务并行，CPU 可达 15x 实时
- 内置 VAD/标点：**无**（需前端 VAD 切句或自己接标点）
- 多语言：中、英、粤语、日、韩等 50+ 语言（含中英混合）——**是 Nano 当前版本未覆盖的语言广度**
- **热词（hotwords）**：✅ **支持**（`hotwords="词1, 词2, 词3"` 参数；也支持热词文件 `词:权重`；ONNX 版 `dengcunqin/SenseVoiceSmall_hotword` 提供独立的 `hotwords_score`）
- **上下文（context）**：❌ **不支持**——非自回归架构，**没有 `initial_prompt` / `context` 参数**；只能通过热词偏向 + 微调实现领域适配，**不能保留跨句对话历史**
- 优点：模型极小、**原生输出情感/事件标签**、端侧 / 嵌入式友好、社区文档更成熟
- 缺点：单次 ≤30s 短音频；无内置 VAD/标点；速度比 Nano 慢约 5 倍；纯 ASR 精度弱于 Nano
- 适用：**需要情感识别 / 声音事件分析**的场景（Nano 不支持）、**要求高并发**的本地部署、端侧 / 嵌入式部署、客户端初步识别

**Whisper（OpenAI / large-v3 / Turbo）**
- 中文效果：英文 > 中文（多语言泛化但中文明显弱于 FunASR 系）
- 推理特征：自回归，长音频耗时显著（30s 音频 ~3-5s on GPU）
- 显存：large-v3 ~3GB（fp16）、Turbo ~1.5GB
- **热词（hotwords）**：❌ **无原生热词接口**——可通过 `prompt` 参数注入关键术语间接实现，但效果有限
- **上下文（context）**：✅ 支持 `initial_prompt` / `prompt`（最长 244 tokens），可注入领域术语 / 风格 / 上文
- 优点：本地部署最成熟、多语言、英文场景首选、社区文档齐全、有 prompt 注入能力
- 缺点：中文精度明显弱于国产系；自回归慢；中文标点恢复弱；无原生 hotwords
- 适用：英文/多语言场景，中文场景**不推荐**


#### 横向对比表

| 维度 | Whisper large-v3 | **FunASR-Nano** | SenseVoice-Small |
|------|------------------|-----------------|------------------|
| 中文效果 | 中 | **优+**（AIShell1 WER 1.80%，开源 SOTA） | 优 |
| 推理速度（30s 音频） | 3-5s（自回归） | **<100ms**（vLLM 340× 实时） | <500ms |
| 模型大小 | ~3GB（FP16） | **~1.5GB（FP16）/ 484MB（GGUF）** | ~470MB（FP16）/ 237MB（Int8） |
| 单次最大音频 | 任意 | **10 分钟** | 30 秒 |
| 显存占用（fp32） | 3～4G | 13～17G | 1.2G左右 |
| 内置 VAD | ❌ | **✅** | ❌（需前端 VAD） |
| 内置标点 | 弱 | **✅**（NT 标点） | ❌ |
| 时间戳 | ❌ | **✅**（Token 级时间戳） | ❌ |
| 情感/事件识别 | ❌ | **❌** | ✅（原生多任务，SER + BED） |
| 多语言 | ✅ 强 | ✅ 中（中文方言 + 英/日） | ✅ 中（50+ 语言） |
| 热词（hotwords） | ❌（仅 prompt 间接） | ✅ **原生**（`hotwords=[...]`） | ✅（`hotwords="词1, 词2"`，也支持热词文件） |
| 上下文（context） | ✅（`initial_prompt` ≤244 tokens） | ✅ **LLM 级**（Qwen3 解码器语义理解） | ❌（非自回归，无 prompt 接口） |
| 部署复杂度 | 低（whisper.cpp / faster-whisper） | **低**（funasr runtime 一行 `AutoModel`） | 低（funasr / ONNX） |
| 社区成熟度 | **高** | 中（2024-2025 新） | 中（2024，文档更全） |


#### 选型建议

| 场景 | 推荐 | 理由 |
|------|------|------|
| **本地 + 中文为主 + 高并发 + 需要情感识别** | **SenseVoice-Small** | Int8 量化 237MB，单卡 30-50+ 路并发；**唯一原生支持 SER/BED**；支持热词（`hotwords=` 参数） |
| **追求极致 ASR 精度 + 需要 LLM 级上下文理解** | FunASR-Nano | AIShell1 WER 1.80%、Qwen3 解码器提供语言模型级上下文、**原生热词 + GGUF 模糊匹配** |
| 端侧 / 嵌入式 / 移动端 | **SenseVoice-Small** | 237MB Int8、CPU 即可跑 |
| 英文为主 / 需要 prompt 注入领域术语 | Whisper large-v3 | 唯一支持 `initial_prompt`；英文 SOTA，文档成熟 |


#### 推荐结论

**首推 SenseVoice-Small**
- **情感识别**：唯一原生支持 SER（happy/angry/sad/neutral/surprise）+ BED（笑声/掌声/背景音乐）的开源 ASR 模型
- **高并发**：显存占用低，单卡 30-50+ 路并发，是 Nano 的 2-3 倍
- **中文支持好**：50+ 语言，比 Nano 的"中英日 + 7 方言"更广


**FunASR-Nano 与 SenseVoice 是互补关系**
- Nano 编码器继承 SenseVoice 编码器（架构 = SenseVoice encoder + Qwen3-0.6B decoder）
- Nano 把解码目标锁定在"高精度文本 + 智能体交互"，放弃多任务输出
- SenseVoice 保留"轻量 + 多任务感知"赛道
- 工程上可**按需切换**：默认 SenseVoice，遇高精度需求时切 Nano

---

#### ASR 上下文传参实践（**仅 FunASR-Nano 原生支持**）

FunASR-Nano 的 `AutoModel.generate()` 接口提供 **4 个原生上下文参数**（`system` / `prompt` / `context` / `chat_history`），可在 Python SDK 内部直接传：

```python
result = model.generate(
    input="audio.wav",
    system="你是一个金融领域的语音识别助手",
    prompt="请关注人名、机构名、产品代号",
    context="用户正在和理财经理讨论招商银行理财产品",
    chat_history=[
        {"role": "user", "content": "我想了解一下招商银行的大额存单"},
        {"role": "assistant", "content": "好的，招商银行三年期大额存单年化利率 2.65%。"},
    ],
    hotwords=["招商银行", "大额存单"],
)
```


### 3.2 流式TTS

> **前提调整**：方案 ②（**非流式 ASR + 流式 LLM + 流式 TTS**）要求 TTS 流式输出——按 LLM token 增量切句后，边合成边吐音频 chunk，浏览器 Web Audio API 边收边播。
>
> 评估优先级：**首包延迟 / 中文效果（含方言）> 协议是否可商用 > 声音克隆能力 > 显存占用 > 自然度 / 情感控制**
>
> 数据来源：[VibeVoice-Realtime-0.5B（HF）](https://huggingface.co/microsoft/VibeVoice-Realtime-0.5B)、[Fish Speech 1.5](https://github.com/fishaudio/fish-speech)、[CosyVoice2-0.5B](https://github.com/FunAudioLLM/CosyVoice)、[IndexTTS-2](https://github.com/index-tts/index-tts)、[Qwen3-TTS CustomVoice](https://huggingface.co/Qwen/Qwen3-TTS-12Hz-1.7B-CustomVoice)、[Faster IndexTTS-2（arXiv 2607.21042）](https://arxiv.org/abs/2607.21042)

#### 候选模型概览

| 模型 | 形态 | 流式特性 | 中文支持 | 协议 | 候选状态 |
|------|------|---------|---------|------|----------|
| **VibeVoice-Realtime-0.5B** | 本地开源 | **流式输入 + 流式输出** | ❌ 官方明确不支持（按拼音逐字读） | MIT（可商用） | ⚠️ 备选（仅英文场景） |
| **Fish Speech 1.5** | 本地开源 | 流式输出（DualAR） | ✅ 中文 MOS 4.6/5、训练数据 > 300k 小时 | CC-BY-NC-SA-4.0（**禁商用**） | ⚠️ 备选（仅自用/研究） |
| **CosyVoice2-0.5B** | 本地开源 | 流式输出 | ✅ 中文 + 方言 + 中英混合 | Apache-2.0（可商用） | ✅ 中文场景主选之一 |
| **IndexTTS-2** | 本地开源 | 流式输出（TensorRT-LLM 加速后） | ✅ 中文 + 多音字纠错 | Apache-2.0（可商用） | ✅ 中文场景主选之一 |
| **Qwen3-TTS-12Hz** | 本地开源 | 流式输出（首包 97ms） | ✅ 中文 + 9 种方言 | Apache-2.0（可商用） | ✅ **首推（中文 + 超低延迟）** |

#### 详细对比

**VibeVoice-Realtime-0.5B（微软，2025）**
- 模型规格：0.5B 参数（基于 Qwen2.5-0.5B），FP16 仅 ~2.2GB / INT8 ~1.1GB / INT4 ~0.6GB 显存
- 核心定位：**首个开源真"流式输入+流式输出" TTS**——文字边输入边合成边播
- 架构：LLM 主干 + **σ-VAE 声学 Tokenizer**（7.5Hz 超低帧率）+ 扩散解码头
- 首字延迟：~300ms（官方），7.5Hz × 4 帧 ≈ 533ms 起步
- 上下文长度：8k tokens，约 **10 分钟**连续语音
- 多语言：⚠️ **官方明确仅英语训练**，中文及其他语言"may be unintelligible"——会按拼音逐字读
- 协议：MIT（可商用）
- 声音克隆：支持参考音频音色
- 部署：Docker / Python venv / OpenAI 兼容 API / WebSocket `ws://.../stream`
- 优点：**真流式输入**（其他模型不具备的差异化能力）、显存极低（INT4 0.6GB）、上下文长达 10 分钟
- 缺点：**不支持中文**（拼音逐字读）；社区中文微调版未成熟
- 适用：**英文场景的实时流式 TTS**；本项目中文场景**直接排除**

**Fish Speech 1.5（fishaudio，2024）**
- 模型规格：**DualAR 双自回归架构**（主 Transformer 21Hz + 次 Transformer 潜在状态转换）；支持 8-bit 量化（模型缩减 30%，保 98%+ 音质）
- 核心定位：多语言、流式生成、低显存
- 推理性能：RTX 4090 + `--compile` 实测 RTF **0.28**（260 token/s），端到端延迟 **< 300ms**；官方宣传 < 150ms
- 显存：RTX 3060 8GB 起，推荐 12GB+；生产建议 A10 24GB
- 中文效果：训练数据 **> 300,000 小时**，中文 MOS **4.6/5**（接近专业播音员），普通话四声调自然过渡、轻声助词自动弱化、中英混排识别准确
- 多语言：中、英、日、韩、阿拉伯等
- 流式：**流式输出**，按 token 增量吐音频片段
- 声音克隆：**10-30 秒参考音频** 即可克隆
- 协议：⚠️ **CC-BY-NC-SA-4.0**（**非商业 — 禁止商用**）
- 限制：单次生成 ≤ ~2000 字符
- 缺点：**协议不可商用**，本项目（生产产品）**直接排除**
- 适用：自用 / 个人项目 / 学术研究 / 非商业产品

**CosyVoice2-0.5B（阿里通义 / FunAudioLLM，2024）**
- 模型规格：**0.5B 参数**（LLM-based + Flow Matching + HiFi-GAN 声码器）；相比 CosyVoice1 推理速度 ~5× 提升
- 核心定位：多语言、流式推理、超低延迟
- 中文效果：原生支持中文（含粤语、四川话、上海话等方言）+ 中英混合；前置 CosyVoice 60000+ 小时中文训练
- 流式：**官方提供 streaming inference**（边生成 token 边合成音频），适合逐句流式合成
- 声音克隆：**3-10 秒参考音频** 即可零样本克隆
- 多语言：中、英、日、韩、粤语等
- 协议：**Apache-2.0**（可商用）
- 部署：funasr runtime 兼容 / 独立 Python 推理；社区 vLLM/Transformers 推理稳定
- 显存：~4-8GB（FP16）
- 优点：中文 SOTA 之一、流式推理成熟、社区/文档完善、可商用
- 缺点：相比 5B 预训练版本效果略弱（0.5B 是平衡版）；声音情感控制弱于 Qwen3-TTS / IndexTTS-2
- 适用：**中文场景主选之一**（生态成熟、社区案例多、可商用）

**IndexTTS-2（B站 IndexTeam，2025-09）**
- 模型规格：**~1.5B 参数**（自回归 GPT 512M + 预处理 788M + DiT 96M + BigVGAN2 113M）
- 架构：GPT-2 + Conformer 条件编码器 + **字符-拼音混合建模**（自动纠正中文发音）+ BigVGAN2 声码器
- 核心定位：首个 **支持精确时长控制** 的自回归 TTS（毫秒级误差 < 0.03%）+ **8 种情感解耦控制**（开心/愤怒/悲伤/害怕/厌恶/忧郁/惊讶/平静）
- 中文效果：55,000 小时多语言语料训练；**多音字纠错**（字符-拼音混合建模）；支持中文/英文/中英混合
- 声音克隆：**5-15 秒参考音频** 零样本克隆；情感可通过参考音频 / 情感向量 / 自然语言描述（Qwen3 微调）三种方式注入
- 采样率：24kHz
- 流式：**Faster IndexTTS-2** 论文（arXiv 2607.21042）通过 TensorRT-LLM 加速 GPT 模块 5.0×、端到端 3.6×；支持**分块流式合成 + Hann 窗交叉淡入淡出**，RTF 从 0.76 → 0.22（中文）
- 显存：最低 6-8GB（fp16 + 短文本），推荐 8-12GB
- 协议：**Apache-2.0**（可商用）；⚠️ 早期文档提及商用需联系 indexspeech@bilibili.com
- 优点：情感控制最细（8 种）、时长可控、声音克隆质量高、中文深度优化
- 缺点：模型偏大（1.5B），单卡显存吃紧；原生流式需自行改造（依赖 Faster IndexTTS-2 加速方案）
- 适用：**影视配音 / 视频音画同步**、有声读物、虚拟主播、**需要情感/时长精确控制的场景**

**Qwen3-TTS（阿里通义，2025）**
- 模型规格：**12Hz 系列 0.6B / 1.7B**；Qwen-TTS-Tokenizer-12Hz（16 层残差矢量量化 RVQ：第 1 层语义、后 15 层渐进编码声学细节）
- 架构：**双轨 LLM**（Dual-Track LLM：语义理解 + 声学建模并行）+ **全因果编码器 + 轻量卷积解码器**（无需 look-ahead，即生成即解码）+ **多令牌预测 MTP** 模块
- 核心定位：**超低延迟 + 多模式** —— Voice Design / Voice Clone / InstructTTS 三种核心能力
- **首包延迟（最大亮点）**：
  - Qwen3-TTS-12Hz-0.6B：**97ms**（1 并发）/ 299ms（6 并发）
  - Qwen3-TTS-12Hz-1.7B：101ms（1 并发）/ 333ms（6 并发）
- RTF：0.434（0.6B）/ 0.463（1.7B）
- 中文效果：✅ **9 种中文方言**深度支持；中文/英文/日文/韩文/德文/法文/俄文/葡文/西文/意文 10 种语言
- 声音克隆：**3 秒参考音频**即可克隆（业界最短）；跨语言克隆错误率仅 **4.82%**（对比 CosyVoice3 14.4%）
- InstructTTS：根据文本语义自动调整语调、语速、情感
- 描述-语音一致性（InstructTTSEval）：**中文 81.1% / 英文 82.4%**，超越 Hume / MiniMax / ElevenLabs
- 显存：建议 8GB+，4GB 可跑但性能受限
- 国产硬件：昇腾 910B（98ms）/ 寒武纪 MLU370-X8（103ms）均已验证稳定
- 协议：**Apache-2.0**（可商用，无授权费）
- ComfyUI 集成：✅
- 优点：**首包延迟 < 100ms**（开源 TTS 第一梯队）、9 种方言、3 秒克隆、跨语言克隆准确率高、可商用
- 缺点：模型较新（2025），生产案例样本少；细粒度情感/时长精确控制不如 IndexTTS-2
- 适用：**实时对话场景的首选**（首包延迟最关键）、声音克隆、智能客服、车载、虚拟主播

#### 横向对比表

| 维度 | VibeVoice-Realtime-0.5B | Fish Speech 1.5 | **CosyVoice2-0.5B** | IndexTTS-2 | **Qwen3-TTS-12Hz** |
|------|------------------------|-----------------|---------------------|-----------|---------------------|
| **参数规模** | 0.5B | DualAR 21Hz | 0.5B | ~1.5B | 0.6B / 1.7B |
| **首包延迟** | ~300ms | <150-300ms | 中等（流式成熟） | 较长（需 TRT 加速） | **97-101ms**（开源最优） |
| **流式输入** | ✅ 唯一支持 | ❌ | ❌ | ❌ | ❌ |
| **流式输出** | ✅ | ✅ | ✅ | ✅（需 Faster 版） | ✅ |
| **中文支持** | ❌（拼音逐字读） | ✅（MOS 4.6） | ✅（含方言） | ✅（多音字纠错） | ✅（**9 种方言**） |
| **声音克隆** | ✅ | ✅（10-30s） | ✅（3-10s） | ✅（5-15s） | ✅（**3s 最短**） |
| **情感控制** | ❌ | ❌ | 弱 | ✅ **8 种** | ✅（InstructTTS） |
| **时长控制** | ❌ | ❌ | ❌ | ✅ **毫秒级** | ❌ |
| **协议** | ✅ MIT | ❌ **CC-BY-NC-SA-4.0** | ✅ Apache-2.0 | ✅ Apache-2.0 | ✅ Apache-2.0 |
| **商用可行性** | ✅ | ❌ | ✅ | ✅ | ✅ |
| **显存（FP16）** | **~2.2GB** | ~8GB+ | ~4-8GB | ~8-12GB | ~8GB+ |
| **部署复杂度** | 中（Docker） | 低（Python WebUI） | 低（funasr runtime） | 中（需 TRT 加速） | 低（`pip install qwen-tts`） |
| **社区成熟度** | 低（2025 新） | 中 | **高** | 中（2025-09） | 中（2025） |
| **采样率** | 24kHz | 44.1kHz | 24kHz | 24kHz | 24kHz |
| **多语言** | 仅英语 | 中/英/日/韩 | 中/英/日/韩/粤语 | 中/英 | **10 种语言** |

#### 选型建议

| 场景 | 推荐 | 理由 |
|------|------|------|
| **中文实时对话 + 超低延迟 + 可商用（本项目主场景）** | **Qwen3-TTS-12Hz-0.6B** | 首包 97ms 开源最优、Apache-2.0、9 种方言、3 秒克隆、显存 ~8GB |
| **英文场景 + 真流式输入（边输文字边出音频）** | **VibeVoice-Realtime-0.5B** | 唯一支持流式输入的开源模型；MIT 协议；INT4 仅 0.6GB 显存 |
| **中文生产部署 + 成熟生态** | **CosyVoice2-0.5B** | 阿里开源、流式推理成熟、社区文档全、可商用 |
| **影视配音 / 情感/时长精确控制** | **IndexTTS-2** | 唯一支持毫秒级时长控制 + 8 种情感解耦；Apache-2.0 |
| **自用 / 研究 / 非商业** | **Fish Speech 1.5** | 中文效果极好（MOS 4.6），但 CC-BY-NC-SA-4.0 禁商用 |

#### 推荐结论

**首推 Qwen3-TTS-12Hz-0.6B**
- **首包延迟 97ms**：开源 TTS 第一梯队，是当前实时对话"边说边听"延迟目标（< 1.5s）最大瓶颈的缓解点（首字延迟这一段通常占总延迟 30-50%）
- **中文 + 9 种方言**：本项目中文场景最契合
- **3 秒声音克隆**：体验端可以做"快速定制音色"功能
- **Apache-2.0**：商用零门槛
- **显存 ~8GB**：与 SenseVoice-Small + Qwen3 LLM 共卡部署可行性高

**备选 1：CosyVoice2-0.5B**
- 中文 SOTA 之一、流式推理成熟、社区案例多、Apache-2.0
- 适合作为"主选模型遇到不可解决问题时的兜底"，或与 Qwen3-TTS A/B 测试

**备选 2：IndexTTS-2**
- 当场景出现"严格时长对齐"（如视频音画同步、字幕配音）或"细粒度情感控制"需求时切换
- 显存压力略大（~8-12GB），需 Faster IndexTTS-2 + TensorRT-LLM 加速才能拉到与 Qwen3-TTS 同等流式水平

**直接排除**
- ❌ **VibeVoice-Realtime-0.5B**：官方明确不支持中文（拼音逐字读），本项目中文场景不适用（仅英文场景备选）
- ❌ **Fish Speech 1.5**：CC-BY-NC-SA-4.0 协议禁止商用，本项目为生产产品故排除